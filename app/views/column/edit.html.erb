<% provide(:title, 'Edit column') %>
<h1>Edit column</h1>

<div>
	<%= form_for(@column) do |f| %>
		<%= f.label :name %>
		<%= f.text_field :name %>

		<%= f.submit "Submit", disable_with: 'Submiting...' %>
	<% end %>
</div>
